// // Check if user is not logged in (mock)
//         const isAuthenticated = false;

//         if (!isAuthenticated) {
//             document.getElementById('spotifyLogin').addEventListener('click', () => {
//                 // Redirect to Spotify OAuth login
//                 window.location.href = 'https://accounts.spotify.com/authorize?client_id=YOUR_CLIENT_ID&response_type=code&redirect_uri=YOUR_REDIRECT_URI&scope=playlist-modify-public';
//             });
//         } else {
//             // Hide CTA if already authenticated
//             document.getElementById('spotifyLogin').style.display = 'none';
//         }
